---
 drivers/net/8139too.c |    6 +++++-
 1 file changed, 5 insertions(+), 1 deletion(-)

Index: linux-2.6.23-rt1/drivers/net/8139too.c
===================================================================
--- linux-2.6.23-rt1.orig/drivers/net/8139too.c	2007-10-11 15:58:11.000000000 -0400
+++ linux-2.6.23-rt1/drivers/net/8139too.c	2007-10-11 15:59:57.000000000 -0400
@@ -2211,7 +2211,11 @@ static irqreturn_t rtl8139_interrupt (in
  */
 static void rtl8139_poll_controller(struct net_device *dev)
 {
-	disable_irq(dev->irq);
+	/*
+	 * use _nosync() variant - might be used by netconsole
+	 * from atomic contexts:
+	 */
+	disable_irq_nosync(dev->irq);
 	rtl8139_interrupt(dev->irq, dev);
 	enable_irq(dev->irq);
 }
