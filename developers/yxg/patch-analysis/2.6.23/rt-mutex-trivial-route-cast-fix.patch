---
 net/ipv4/route.c |    2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

Index: linux-2.6.23-rt1/net/ipv4/route.c
===================================================================
--- linux-2.6.23-rt1.orig/net/ipv4/route.c	2007-10-11 15:57:54.000000000 -0400
+++ linux-2.6.23-rt1/net/ipv4/route.c	2007-10-11 16:00:25.000000000 -0400
@@ -237,7 +237,7 @@ static spinlock_t	*rt_hash_locks;
 			spin_lock_init(&rt_hash_locks[i]); \
 		}
 #else
-# define rt_hash_lock_addr(slot) NULL
+# define rt_hash_lock_addr(slot) ((spinlock_t *)NULL)
 # define rt_hash_lock_init()
 #endif
 
