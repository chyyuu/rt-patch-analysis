---
 kernel/cpu.c |    2 ++
 1 file changed, 2 insertions(+)

Index: linux-2.6.23-rt1/kernel/cpu.c
===================================================================
--- linux-2.6.23-rt1.orig/kernel/cpu.c	2007-10-11 15:57:35.000000000 -0400
+++ linux-2.6.23-rt1/kernel/cpu.c	2007-10-11 16:00:58.000000000 -0400
@@ -37,12 +37,14 @@ void lock_cpu_hotplug(void)
 	struct task_struct *tsk = current;
 
 	if (tsk == recursive) {
+#ifdef CONFIG_PREEMPT_RT
 		static int warnings = 10;
 		if (warnings) {
 			printk(KERN_ERR "Lukewarm IQ detected in hotplug locking\n");
 			WARN_ON(1);
 			warnings--;
 		}
+#endif
 		recursive_depth++;
 		return;
 	}
